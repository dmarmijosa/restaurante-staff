# Copia este archivo a `.env` y completa los valores desde
# Supabase Dashboard → Project Settings → API. Nunca subas `.env` al repositorio.
SUPABASE_URL=https://vtkdvxrocemdyybynegs.supabase.co
SUPABASE_ANON_KEY=
